version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd client-apps
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: client-apps/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
